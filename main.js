var landExplored = 0;
var exploreCap = 1;
var landps = 0;

venture(number, whodunit){
  if (whodunit = "player") {
    landExplored = landExplored + exploreCap;
  };
  else landExplored = landExplored + landps
};
